{
  "name": "agentchain",
  "version": "0.2.0",
  "private": true,
  "description": "Local orchestrator for chaining authenticated Claude Code and Codex CLI sessions.",
  "type": "module",
  "bin": {
    "agentchain": "./dist/src/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.18",
    "@types/react": "^19.2.14",
    "typescript": "^5.8.3",
    "vitest": "^3.1.3"
  },
  "dependencies": {
    "@assistant-ui/react": "^0.14.0",
    "@assistant-ui/react-ink": "^0.0.15",
    "ink": "^7.0.3",
    "react": "^19.2.6"
  }
}
